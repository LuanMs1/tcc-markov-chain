from typing import Tuple
def get_upper_triangle_indices(N: int, k: int) -> Tuple[int, int]:
    """get the size of the flatten array and map to the correspondent position in the 2d matrix upper right"""
    
    return(0,0)
